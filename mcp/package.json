{
  "name": "led-mcp",
  "version": "0.1.0",
  "private": true,
  "description": "인생관리AI앱 외부 API(/api/v1)를 클로드 코드에서 쓰는 MCP 서버 (stdio)",
  "type": "module",
  "bin": {
    "led-mcp": "server.js"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "scripts": {
    "test": "node --test"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.30.0",
    "zod": "^4.6.5"
  }
}
